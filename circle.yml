# Customize the test machine
machine:
  node:
    version: 4.1.0
  ruby:
    version: 2.1.2
  php:
    version: 5.6.2

  # Override /etc/hosts
  services:
    - docker

dependencies:
  pre:
   - chmod +x -R scripts/*
   #- tooling/scripts/install_laravel_app_dependencies.sh
   - sh scripts/install-swagger-codegen-circleci.sh
   #- sh tooling/swagger_sdks/generate-v2-sdks.sh
   - sh scripts/generate-v1-sdks-circleci.sh

  override:
    # Create a .env file with development settings
    #- tooling/scripts/development/create_circleci_environment.sh

#test:
#  override:
#    - echo "This line is to prevent automatic test detection."

general:
  artifacts:
    #- "swagger-codegen/quantimodo_v2_skds.zip"
    - "swagger-codegen/quantimodo_v1_skds.zip"
    - "swagger-codegen/api-docs.json"